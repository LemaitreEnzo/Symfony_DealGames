{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Ad</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Appliquer</button>
    {{ form_end(form) }}
{% endblock %}
